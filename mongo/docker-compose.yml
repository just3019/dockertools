version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    volumes:
      - mongo_data:/data/db
    # environment:
    #   MONGO_INITDB_ROOT_USERNAME: mongo
    #   MONGO_INITDB_ROOT_PASSWORD: mongo
    ports: 
      - "27017:27017"
  
volumes:
  mongo_data: